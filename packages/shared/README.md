# Shared Package

This package contains shared code between the web and mobile applications.

## Structure

- `types/` - Shared TypeScript types
- `api/` - API client wrappers
- `utils/` - Utility functions

## Usage

Import types from the shared package in your web or mobile application:

```typescript
import type { User } from '@learning-platform/shared/types'
```